# pyproject.toml
[project]
name = "Rikannodes" # Unique identifier for your node. Immutable after creation.
description = "mixed kijai ComfyUI-PromptRelay with DragonDiffusionbyBoyo, add clip input and output in lora gate, add fps and max frame out in timeline"
version = "1.0" # Custom Node version. Must be semantically versioned.
license = { file = "LICENSE.txt" }
dependencies  = [] # Filled in from requirements.txt

[project.urls]
Repository = "https://github.com/rikanrino/Rikannodes/"

[tool.comfy]
PublisherId = "rikanrino" # TODO (fill in Publisher ID from Comfy Registry Website).
DisplayName = "rikanrino" # Display name for the Custom Node. Can be changed later.
Icon = "https://example.com/icon.png" # SVG, PNG, JPG or GIF (MAX. 800x400px)
