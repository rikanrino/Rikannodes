# pyproject.toml
[project]
name = "Rikan-PromptRelay" # Unique identifier for your node. Immutable after creation.
description = ""
version = "1.0.2" # Custom Node version. Must be semantically versioned.
license = { file = "LICENSE.txt" }
dependencies  = [] # Filled in from requirements.txt

[project.urls]
Repository = "https://github.com/rikanrino/Rikan-PromptRelay/"

[tool.comfy]
PublisherId = "rikanrino" # TODO (fill in Publisher ID from Comfy Registry Website).
DisplayName = "Rikan-PromptRelay" # Display name for the Custom Node. Can be changed later.
Icon = "https://example.com/icon.png" # SVG, PNG, JPG or GIF (MAX. 800x400px)
